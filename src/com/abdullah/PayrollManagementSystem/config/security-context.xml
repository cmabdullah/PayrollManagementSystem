<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<security:authentication-manager>
		<security:authentication-provider>
			<security:user-service>
				<security:user name="abdullah" authorities="admin"
					password="123" />
				<security:user name="abida" authorities="admin"
					password="123" />
			</security:user-service>
		</security:authentication-provider>
		
		<!--added new authontication provider where reference datasource = dataSource -->
		<security:authentication-provider>
			<security:jdbc-user-service data-source-ref="dataSource" />
		</security:authentication-provider>
		
		
	</security:authentication-manager>
	
<!-- 
where this query is written i dont know
Your login attempt was not successful, try again.

Reason: PreparedStatementCallback; bad SQL grammar [select username,authority from authorities where username = ?]; nested exception is java.sql.SQLSyntaxErrorException: Table 'PayrollManagementSystem.authorities' doesn't exist


 -->
	
	
	
	<security:http use-expressions="true">
		<security:form-login />
		<security:intercept-url pattern="/" access="permitAll" />
		<!--change default spring login path-->
		<security:form-login login-page="/login"
			authentication-failure-url="/login?error=true" />
		<security:intercept-url pattern="/usersinfo" access="permitAll" />
		<!--Permit all static resources like bootstrup library and so one...-->
		<security:intercept-url pattern="/static/**" access="permitAll" />
		<!--Give login url permission-->
		<security:intercept-url pattern="/login" access="permitAll" />
		<security:intercept-url pattern="/registration" access="isAuthenticated()" />
		<security:intercept-url pattern="/docreate" access="isAuthenticated()" />
		<security:intercept-url pattern="/**" access="denyAll" />
	</security:http>
	
	
</beans>
